Django==5.1.3
gunicorn  # Required for running Django on Railway
psycopg2-binary  # Required if using PostgreSQL
whitenoise  # For serving static files
dj-database-url  # If using a DATABASE_URL environment variabledjango-extensions
django-extensions
